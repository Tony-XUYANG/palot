---
"@palot/desktop": minor
---

Add the optional Palot Cloud prepaid provider with strict OS-encrypted credentials, a loopback-only OpenCode proxy, balance and usage settings, and a disabled-by-default production manifest until the managed gateway passes deployment acceptance.
